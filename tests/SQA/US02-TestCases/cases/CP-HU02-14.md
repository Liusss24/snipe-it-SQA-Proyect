# CP-HU02-14

## Metadata

- **ID:** CP-HU02-14
- **Title:** Intento de checkout por un usuario sin permisos
- **Module:** Activos – Checkout / Control de permisos
- **US:** HU02
- **Type:** Integración
- **Technique:** Control de permisos
- **Execution:** Automatizada – PHP 8.3 + Pest
- **Priority:** High
- **Hardware:** Laptop Lenovo ideapad 3
- **OS:** Windows 11
- **Browser:** Chrome – 1920×1080
- **Assigned to:** María Paula Castillo Chinchilla

## Preconditions

- Existe un usuario sin permisos de checkout (`viewer` / `SQA-Viewer2026!`), con su API token configurado en `.env` como `SNIPEIT_NOPERM_TOKEN`.
- Existe un activo con estado Ready to Deploy no asignado.
- Existe un usuario válido como receptor del activo.

## Steps

1. Crear activo y usuario receptor vía API (fixture, usando token admin).
2. Enviar `POST /api/v1/hardware/{id}/checkout` con el token del usuario `viewer` (sin permisos de checkout).
3. Verificar el código HTTP y el cuerpo de la respuesta.
4. Consultar el estado y asignación del activo (usando token admin).

## Test data

- Token: `SNIPEIT_NOPERM_TOKEN` (usuario viewer sin permisos de checkout).
- Activo: creado dinámicamente con estado Ready to Deploy.
- Usuario receptor: `Receptor Valido` (creado dinámicamente).

## Expected result

- El sistema deniega el acceso con HTTP 401 o 403.
- `status` no es `"success"`.
- El activo permanece en estado Ready to Deploy, sin asignación.

## Actual result

Ejecutado el 2026-05-31 (automatizado). Con el token del usuario `viewer` (sin permiso
`assets.checkout`), el sistema devolvió HTTP 403. El activo permaneció en estado
Ready to Deploy sin asignación.

## Status

- [x] Pass  [ ] Fail  [ ] Blocked

## Evidence

- File(s): `reports/2026-05-31_US02_execution_report.txt`
- Notes: Test `CP-HU02-14` en `automated/CP_HU02_14_sin_permisos.php`. Usuario viewer (id=2) con `assets.checkout=0`.

## Defect link (if fail)

-
